Use $folloze-digital-deal-room-internal.

Operator: Luke
Account: Euna Solutions
Meeting: this morning's Folloze demo call
Inputs: the attached Kaia download package containing the call transcript and recording

Build an internal, buyer-safe Folloze Resource Center from Board 248623 using the direct Folloze API/native-template workflow.

Production target: complete the warmed path in 5–8 minutes and never continue past 10 minutes. Start by running the skill's complete preflight concurrently. If the Folloze Experience API, board-editor invitation path, Slack recipient, required source package, tracker, tile library, or local media tools are not ready, stop within 60 seconds before copying a board and return the exact blocker. Do not repair OAuth, inspect app bundles/source maps, troubleshoot browser extensions, or discover endpoints during this production run.

Before any board mutation:

1. Inspect only the supplied Kaia package or extracted handoff directory for the transcript and recording.
2. Validate that both files match Euna Solutions and this morning's call.
3. If both are valid, use them locally and do not open Kaia, inspect email, request another export, or use browser automation for acquisition.
4. If a required asset is absent, stop and tell me exactly what is missing before using the browser fallback.
5. Trim opening small talk only when the transcript or opening review supports a real cut, then verify the new file, duration, streams, hashes, opening content, and playback.

For the middle value section, preserve the existing four-card layout and order:

1. Build Speed & Scalability
2. Personalization & Enrichment
3. Analytics & Optimization
4. Openness & AI Connectivity only if the call meaningfully discussed connecting Folloze to Claude, ChatGPT, Gemini, MCP, an agent/LLM, or another external AI system. Otherwise use Operational Scale & Reuse with no AI language.

Keep the category labels/headings when they already match. Use transcript evidence to personalize the overall section subheader plus each card's subheadline/body and proof line. Keep transcript citations private.

For the Example Folloze Boards section:

1. Read the `MCP Demo Environments - May 2026` Google Sheet, tab `Demo Environments`, and find the Euna Solutions demo-pack rows using bounded header, company-key, and exact-row reads.
2. Use the production Deployment URL from the sheet as each buyer-facing destination; keep Designer edit URLs private.
3. Preserve the six baseline template examples, then append the verified Euna campaign, account/expansion, and event examples in that order.
4. Resolve each preview from the company-wide Drive folder `Folloze Demo Example Tile Library`: https://drive.google.com/drive/folders/1dTAsMtmopoSgTVQY4JXnhwRh7S28ZXFb. If my correct local tile is not there, upload a copy using `[board-id]-[normalized-company]-[normalized-motion]-tile.png`, then use the Drive-backed copy as the shared source.
5. Do not substitute screenshots or generic recording/deck covers for missing tiles; report a blocked example instead.

Resolve Luke Rafferty's exact current organization user/invite identity during preflight; do not assume a specific email alias. Publish only after the recording, deck, brand, value-framework, visual-QA, vanity-URL, and readback gates pass. Verify Luke is an editor. Do not share the room with the customer; stop at internal review-ready and return the designer URL, vanity public URL, board ID, asset evidence, value-card evidence map, and blockers.

Use one account-neutral runner and one bounded final verifier. Keep API, Drive, and Sheet responses field-scoped; do not print full configs, organizations, sheets, DOM trees, or source maps. Return total wall-clock time, runtime profile, stage timings, timeout status, token usage when available, and an estimated cost only when a defensible model/rate basis is available. Otherwise return null cost with the reason it cannot be calculated.
